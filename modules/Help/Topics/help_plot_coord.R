help_plot_coord_ui <- function(id) {
  ns <- shiny::NS(id)
  
  htmltools::tagList(
    "The coordinate layer is currently not implemented."
  )
}

help_plot_coord <- function(
  input, output, session, .values
) {
  
  ns <- session$ns
}